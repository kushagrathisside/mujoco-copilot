# mujoco-copilot frontend

React + Vite interface for `mujoco-copilot`.

## Commands

```bash
npm install
npm run dev
npm run build
```

For the full one-command app startup, run `./run-dev.sh` from the repository root.
